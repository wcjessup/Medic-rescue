{
  "name": "paramedic-rescue",
  "version": "0.1.0",
  "private": true,
  "description": "A Pokemon-style paramedic simulation game (TypeScript + Canvas)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build:artifact": "node scripts/build-artifact.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  }
}
